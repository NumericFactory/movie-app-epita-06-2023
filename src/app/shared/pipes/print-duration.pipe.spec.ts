import { PrintDurationPipe } from './print-duration.pipe';

describe('PrintDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
